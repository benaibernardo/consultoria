<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apresentação - Benai Bernardo</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;800&family=Outfit:wght@400;700;900&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <style>
        :root {
            --bg-start: #1e3a8a;
            --bg-end: #0f172a;
            --surface: rgba(30, 41, 59, 0.6);
            --primary: #f8fafc;
            --text-muted: #cbd5e1;
            --accent-cyan: #06b6d4;
            --accent-blue: #3b82f6;
            --gold: #fbbf24;
            --orange: #f97316;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Inter', sans-serif; }
        
        body {
            background-color: #e2e8f0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            color: var(--primary);
        }

        .presentation-card {
            width: 100%;
            max-width: 420px;
            background: linear-gradient(145deg, var(--bg-start) 0%, var(--bg-end) 100%);
            min-height: 100vh;
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
            border: 1px solid rgba(251, 191, 36, 0.2);
        }

        @media (min-width: 421px) {
            .presentation-card {
                min-height: 850px;
                border-radius: 24px;
                margin: 20px 0;
            }
        }

        .bg-glow-cyan {
            position: absolute; top: -5%; left: -15%; width: 300px; height: 300px;
            background: var(--accent-cyan); filter: blur(100px); border-radius: 50%; z-index: 0; opacity: 0.3;
        }
        .bg-glow-orange {
            position: absolute; bottom: 30%; right: -20%; width: 350px; height: 350px;
            background: var(--orange); filter: blur(120px); border-radius: 50%; z-index: 0; opacity: 0.25;
        }
        .bg-glow-blue {
            position: absolute; bottom: -10%; left: 10%; width: 250px; height: 250px;
            background: var(--accent-blue); filter: blur(90px); border-radius: 50%; z-index: 0; opacity: 0.4;
        }

        .content-wrapper {
            position: relative;
            z-index: 1;
            padding: 40px 25px;
            display: flex;
            flex-direction: column;
            flex: 1;
        }

        .header-brand {
            text-align: center;
            margin-bottom: 25px;
            margin-top: 10px;
        }
        .brand-title {
            font-family: 'Outfit', sans-serif;
            font-size: 2.4rem;
            font-weight: 900;
            color: #ffffff;
            letter-spacing: 1px;
            line-height: 1.1;
            text-shadow: 0 4px 15px rgba(0,0,0,0.3);
            cursor: pointer; 
            user-select: none; 
            -webkit-tap-highlight-color: transparent;
        }
        .brand-line {
            height: 4px;
            width: 70px;
            background: #0ea5e9;
            margin: 15px auto;
            border-radius: 4px;
            box-shadow: 0 2px 10px rgba(14, 165, 233, 0.4);
        }
        .brand-subtitle {
            font-family: 'Inter', sans-serif;
            font-size: 0.85rem;
            font-weight: 600;
            color: var(--text-muted);
            letter-spacing: 3px;
            text-transform: uppercase;
        }
        .brand-catchphrase {
            margin-top: 20px;
            font-size: 1.05rem;
            color: #ffffff;
            font-weight: 300;
            font-style: italic;
            line-height: 1.5;
            opacity: 0.95;
        }

        /* Tabs (Múltiplas Propostas) */
        .tabs-container {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }
        
        @keyframes pulseGlow {
            0% { box-shadow: 0 0 8px rgba(249, 115, 22, 0.3); }
            50% { box-shadow: 0 0 18px rgba(249, 115, 22, 0.6); }
            100% { box-shadow: 0 0 8px rgba(249, 115, 22, 0.3); }
        }

        .tab-btn {
            background: rgba(249, 115, 22, 0.1);
            border: 1px solid rgba(249, 115, 22, 0.4);
            color: #fff;
            padding: 10px 20px;
            border-radius: 30px;
            font-size: 0.95rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            user-select: none;
            -webkit-tap-highlight-color: transparent;
            animation: pulseGlow 2.5s infinite;
        }
        .tab-btn:hover {
            background: rgba(249, 115, 22, 0.2);
            transform: translateY(-2px);
        }
        .tab-btn.active {
            background: linear-gradient(to right, var(--orange), #ea580c);
            color: #fff;
            border-color: var(--orange);
            box-shadow: 0 0 25px rgba(249, 115, 22, 0.7);
            transform: scale(1.05);
            animation: none; /* Remove o pulso quando está ativo */
        }

        /* Animação Tátil (Flip) */
        .pricing-wrapper {
            perspective: 1000px;
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        .pricing-section {
            transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.4s ease;
            transform-style: preserve-3d;
            transform-origin: center center;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .pricing-title {
            font-family: 'Outfit', sans-serif;
            font-size: 1.2rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            text-align: center;
            margin-bottom: 25px;
            color: var(--gold);
            display: flex; align-items: center; justify-content: center; gap: 10px;
        }
        .pricing-title::before, .pricing-title::after {
            content: ''; height: 1px; width: 30px; background: var(--gold); opacity: 0.5;
        }

        .plan-card {
            background: var(--surface);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 16px;
            padding: 22px 20px;
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }
        .plan-info h3 { font-size: 1.2rem; font-weight: 800; margin-bottom: 4px; color: #fff; }
        .plan-info p { font-size: 0.85rem; color: var(--text-muted); }
        
        .plan-price { text-align: right; }
        .monthly-price { color: #fff; margin-bottom: 4px; display: flex; align-items: baseline; justify-content: flex-end; gap: 4px; }
        .monthly-price .currency { font-size: 0.9rem; color: var(--text-muted); }
        .monthly-price .amount { font-family: 'Outfit', sans-serif; font-size: 2rem; font-weight: 900; outline: none; }
        .monthly-price .period { font-size: 0.8rem; color: var(--text-muted); }
        
        .hourly-price { font-size: 0.9rem; color: var(--accent-cyan); font-weight: 600; }
        .hourly-price .amount-hour { outline: none; }
        .per-person-text { font-size: 0.75rem; color: var(--gold); margin-top: 4px; font-weight: 600; }

        .plan-card.highlight {
            background: linear-gradient(145deg, rgba(30, 41, 59, 0.95), rgba(15, 23, 42, 0.95));
            border: 1px solid rgba(249, 115, 22, 0.5);
            box-shadow: 0 10px 30px rgba(249, 115, 22, 0.15);
        }
        .plan-card.highlight::before {
            content: ''; position: absolute; top: 0; left: 0; width: 5px; height: 100%;
            background: linear-gradient(to bottom, var(--gold), var(--orange));
        }
        .plan-card.highlight .monthly-price .amount { color: var(--gold); }
        .plan-card.highlight .hourly-price { color: var(--orange); }

        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 0.8rem;
            color: var(--text-muted);
            opacity: 0.9;
            line-height: 1.6;
            background: rgba(0,0,0,0.25);
            padding: 15px;
            border-radius: 12px;
            border: 1px solid rgba(255,255,255,0.05);
        }

        /* Controles */
        .controls-container {
            position: absolute;
            top: 15px; right: 15px;
            display: none;
            gap: 15px;
            z-index: 100;
        }
        .control-btn {
            color: rgba(255,255,255,0.6);
            font-size: 1.2rem;
            cursor: pointer;
            transition: 0.3s;
            background: rgba(0,0,0,0.4);
            width: 40px; height: 40px;
            border-radius: 50%;
            display: flex; justify-content: center; align-items: center;
            border: 1px solid rgba(255,255,255,0.1);
        }
        .control-btn:hover { color: #fff; background: rgba(0,0,0,0.8); border-color: var(--orange); }

        .control-panel {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(15, 23, 42, 0.98);
            backdrop-filter: blur(20px);
            z-index: 99;
            padding: 40px 25px;
            display: none;
            flex-direction: column;
            transform: translateY(-100%);
            transition: transform 0.3s ease-in-out;
            overflow-y: auto;
        }
        .control-panel.active { display: flex; transform: translateY(0); }
        .cp-header {
            display: flex; justify-content: space-between; align-items: center;
            margin-bottom: 20px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 15px;
        }
        .cp-header h2 { font-size: 1.2rem; font-family: 'Outfit'; color: #fff; }
        .cp-close { cursor: pointer; font-size: 1.5rem; color: #fff; }
        
        .cp-group { margin-bottom: 15px; }
        .cp-group label { display: block; font-size: 0.85rem; color: #fff; margin-bottom: 6px; font-weight: 600; }
        .cp-group input {
            width: 100%; padding: 12px; border-radius: 8px;
            background: rgba(0,0,0,0.4); border: 1px solid rgba(255,255,255,0.2);
            color: white; font-size: 1rem; outline: none;
        }
        .cp-group input:focus { border-color: var(--orange); }
        
        .cp-btn {
            width: 100%; padding: 15px; border-radius: 8px;
            background: linear-gradient(to right, var(--orange), #ea580c); color: white; border: none;
            font-weight: 700; font-size: 1rem; cursor: pointer; margin-top: 10px;
            transition: 0.2s;
        }
        .cp-btn:hover { filter: brightness(1.1); }
        .cp-btn:active { transform: scale(0.98); }

        .cp-btn-outline {
            width: 100%; padding: 12px; border-radius: 8px;
            background: transparent; color: #ef4444; border: 1px solid rgba(239, 68, 68, 0.3);
            font-weight: 600; font-size: 0.9rem; cursor: pointer; margin-top: 10px;
            transition: 0.2s;
        }
        .cp-btn-outline:hover { background: rgba(239, 68, 68, 0.1); }

        [contenteditable="true"]:focus {
            border-bottom: 1px dashed var(--orange);
            background: rgba(255,255,255,0.1);
            border-radius: 4px;
            padding: 0 4px;
        }

        #toast {
            position: fixed; bottom: 30px; left: 50%; transform: translateX(-50%) translateY(150px);
            background: var(--accent-blue); color: white; padding: 12px 24px; border-radius: 30px;
            font-weight: 600; font-size: 0.9rem; box-shadow: 0 10px 25px rgba(0,0,0,0.3);
            transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275); z-index: 1000; pointer-events: none;
        }
        #toast.show { transform: translateX(-50%) translateY(0); }
    </style>
</head>
<body>

    <div class="presentation-card" id="capture-area">
        <div class="bg-glow-cyan"></div>
        <div class="bg-glow-orange"></div>
        <div class="bg-glow-blue"></div>

        <div class="controls-container" id="action-controls">
            <div class="control-btn" onclick="generateLink()" title="Copiar Link e Abrir TinyURL">
                <i class="fa-solid fa-link"></i>
            </div>
            <div class="control-btn" onclick="exportImage()" title="Salvar como Imagem (PNG)">
                <i class="fa-solid fa-camera"></i>
            </div>
            <div class="control-btn" onclick="togglePanel()" title="Ajustar Valores">
                <i class="fa-solid fa-sliders"></i>
            </div>
        </div>

        <div class="content-wrapper">
            <div class="header-brand">
                <h1 class="brand-title" onclick="handleSecretClick()" title="Toque 3x para ativar/desativar edição">BENAI BERNARDO</h1>
                <div class="brand-line"></div>
                <h2 class="brand-subtitle">Consultoria de Treinamento</h2>
                <p class="brand-catchphrase">"Sua saúde e performance, no conforto do seu ambiente."</p>
            </div>

            <!-- Tabs de Propostas -->
            <div class="tabs-container" id="tabs-container"></div>

            <div class="pricing-wrapper">
                <div class="pricing-section" id="pricing-section">
                    <div class="pricing-title">Planos <span id="dynamic-target">Individual</span></div>

                    <div class="plan-card">
                        <div class="plan-info">
                            <h3>1x na semana</h3>
                            <p>4 aulas / mês</p>
                        </div>
                        <div class="plan-price">
                            <div class="monthly-price">
                                <span class="currency">R$</span>
                                <span class="amount" id="val-1x-month">320,00</span>
                                <span class="period">/mês</span>
                            </div>
                            <div class="hourly-price">
                                R$ <span class="amount-hour" id="val-1x-hour">80,00</span> /hora
                            </div>
                            <div class="per-person-text" id="pp-1x" style="display:none;"></div>
                        </div>
                    </div>

                    <div class="plan-card highlight">
                        <div class="plan-info">
                            <h3>2x na semana</h3>
                            <p>8 aulas / mês</p>
                        </div>
                        <div class="plan-price">
                            <div class="monthly-price">
                                <span class="currency">R$</span>
                                <span class="amount" id="val-2x-month">560,00</span>
                                <span class="period">/mês</span>
                            </div>
                            <div class="hourly-price">
                                R$ <span class="amount-hour" id="val-2x-hour">70,00</span> /hora
                            </div>
                            <div class="per-person-text" id="pp-2x" style="display:none;"></div>
                        </div>
                    </div>

                    <div class="plan-card">
                        <div class="plan-info">
                            <h3>3x na semana</h3>
                            <p>12 aulas / mês</p>
                        </div>
                        <div class="plan-price">
                            <div class="monthly-price">
                                <span class="currency">R$</span>
                                <span class="amount" id="val-3x-month">720,00</span>
                                <span class="period">/mês</span>
                            </div>
                            <div class="hourly-price">
                                R$ <span class="amount-hour" id="val-3x-hour">60,00</span> /hora
                            </div>
                            <div class="per-person-text" id="pp-3x" style="display:none;"></div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="footer">
                * Atendimento incluso em um raio de até 5km.<br>
                <span style="color: var(--orange); font-weight: 600;">Acima do raio de 5 km, é cobrada uma taxa extra de R$ 25,00 por questões de deslocamento.</span>
            </div>
        </div>

        <!-- PAINEL DE CONTROLE -->
        <div class="control-panel" id="control-panel">
            <div class="cp-header">
                <h2>Ajuste de Valores</h2>
                <i class="fa-solid fa-xmark cp-close" onclick="togglePanel()"></i>
            </div>

            <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; background: rgba(0,0,0,0.2); padding: 12px; border-radius: 12px;">
                <span style="color: #fff; font-size: 0.9rem; font-weight: 600;">Propostas Ativas:</span>
                <button onclick="addProposal()" style="background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2); color: #fff; padding: 6px 12px; border-radius: 20px; font-size: 0.8rem; cursor: pointer; transition: 0.2s;">+ Nova Proposta</button>
            </div>

            <div class="cp-group">
                <label>Nome do Aluno / Proposta</label>
                <input type="text" id="cfg-student-name" placeholder="Ex: João" oninput="updateCurrentProposalName()">
            </div>

            <div class="cp-group">
                <label>Quantidade de Pessoas</label>
                <input type="number" id="cfg-people" min="1" oninput="calculate()">
            </div>

            <div class="cp-group">
                <label>Valor Base Hora/Aula (R$)</label>
                <input type="number" id="cfg-base" oninput="calculate()">
            </div>

            <div class="cp-group">
                <label>Desconto para Grupo (R$ por pessoa na aula)</label>
                <input type="number" id="cfg-group-discount" oninput="calculate()">
            </div>

            <div class="cp-group" style="display:flex; gap:10px;">
                <div style="flex:1;">
                    <label>Desconto 2x/sem (R$)</label>
                    <input type="number" id="cfg-desc-2x" oninput="calculate()">
                </div>
                <div style="flex:1;">
                    <label>Desconto 3x/sem (R$)</label>
                    <input type="number" id="cfg-desc-3x" oninput="calculate()">
                </div>
            </div>

            <button class="cp-btn" onclick="togglePanel()">Aplicar e Fechar</button>
            <button class="cp-btn-outline" id="btn-remove-proposal" onclick="removeCurrentProposal()" style="display:none;">Remover Esta Proposta</button>
        </div>
    </div>

    <script>
        // --- ESTADO DA APLICAÇÃO ---
        function generateId() { return Math.random().toString(36).substr(2, 9); }
        
        let proposals = [];
        let activeId = null;
        let editorMode = false;

        function initDefaultProposal() {
            return {
                id: generateId(),
                name: 'Proposta 1',
                cfg: { people: 1, base: 80, groupDiscount: 10, desc2x: 10, desc3x: 20 },
                display: { m1: '320,00', h1: '80,00', m2: '560,00', h2: '70,00', m3: '720,00', h3: '60,00' }
            };
        }

        // --- RENDERIZAÇÃO E ANIMAÇÃO ---
        function renderTabs() {
            const container = document.getElementById('tabs-container');
            if (proposals.length <= 1) {
                // Se só tem 1 proposta e o nome for genérico, esconde as abas.
                // Se tiver nome, mostra como uma "Tag" de identificação.
                if(proposals[0].name === 'Proposta 1' || proposals[0].name === '') {
                    container.style.display = 'none';
                } else {
                    container.style.display = 'flex';
                    container.innerHTML = `<div class="tab-btn active" style="cursor:default;">Proposta para: ${proposals[0].name}</div>`;
                }
                return;
            }

            container.style.display = 'flex';
            container.innerHTML = '';
            proposals.forEach(p => {
                const btn = document.createElement('div');
                btn.className = 'tab-btn' + (p.id === activeId ? ' active' : '');
                btn.innerText = p.name || 'Sem Nome';
                btn.onclick = () => switchTab(p.id);
                container.appendChild(btn);
            });
        }

        function switchTab(id) {
            if (id === activeId) return;
            
            // Salva edições manuais antes de trocar
            updateActiveFromDOM();
            
            activeId = id;
            const section = document.getElementById('pricing-section');
            
            // Animação de saída (Flip Out)
            section.style.transform = 'rotateY(90deg) scale(0.95)';
            section.style.opacity = '0';
            
            // Vibração leve (se suportado)
            if (navigator.vibrate) navigator.vibrate(15);
            
            setTimeout(() => {
                loadProposalToDOM();
                renderTabs();
                
                // Prepara para entrada
                section.style.transition = 'none';
                section.style.transform = 'rotateY(-90deg) scale(0.95)';
                void section.offsetWidth; // Force reflow
                
                // Animação de entrada (Flip In)
                section.style.transition = 'transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), opacity 0.4s ease';
                section.style.transform = 'rotateY(0deg) scale(1)';
                section.style.opacity = '1';
            }, 400);
        }

        function loadProposalToDOM() {
            const p = proposals.find(p => p.id === activeId);
            if(!p) return;

            // Carrega display
            document.getElementById('val-1x-month').innerText = p.display.m1;
            document.getElementById('val-1x-hour').innerText = p.display.h1;
            document.getElementById('val-2x-month').innerText = p.display.m2;
            document.getElementById('val-2x-hour').innerText = p.display.h2;
            document.getElementById('val-3x-month').innerText = p.display.m3;
            document.getElementById('val-3x-hour').innerText = p.display.h3;

            // Atualiza Título
            const targetEl = document.getElementById('dynamic-target');
            if(p.cfg.people === 1) targetEl.innerText = "Individual";
            else if(p.cfg.people === 2) targetEl.innerText = "Casal / Dupla";
            else targetEl.innerText = `Grupo (${p.cfg.people} pessoas)`;

            // Atualiza "Por pessoa"
            const ppTexts = document.querySelectorAll('.per-person-text');
            if(p.cfg.people > 1) {
                const parseMoney = (str) => parseFloat(str.replace(/\./g, '').replace(',', '.'));
                const formatMoney = (val) => val.toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
                
                document.getElementById('pp-1x').innerText = `(R$ ${formatMoney(parseMoney(p.display.h1)/p.cfg.people)}/h por pessoa)`;
                document.getElementById('pp-2x').innerText = `(R$ ${formatMoney(parseMoney(p.display.h2)/p.cfg.people)}/h por pessoa)`;
                document.getElementById('pp-3x').innerText = `(R$ ${formatMoney(parseMoney(p.display.h3)/p.cfg.people)}/h por pessoa)`;
                ppTexts.forEach(el => el.style.display = 'block');
            } else {
                ppTexts.forEach(el => el.style.display = 'none');
            }

            // Carrega inputs do painel
            document.getElementById('cfg-student-name').value = p.name;
            document.getElementById('cfg-people').value = p.cfg.people;
            document.getElementById('cfg-base').value = p.cfg.base;
            document.getElementById('cfg-group-discount').value = p.cfg.groupDiscount;
            document.getElementById('cfg-desc-2x').value = p.cfg.desc2x;
            document.getElementById('cfg-desc-3x').value = p.cfg.desc3x;

            // Botão de remover
            document.getElementById('btn-remove-proposal').style.display = proposals.length > 1 ? 'block' : 'none';
        }

        function updateActiveFromDOM() {
            const p = proposals.find(p => p.id === activeId);
            if(!p) return;
            p.display.m1 = document.getElementById('val-1x-month').innerText;
            p.display.h1 = document.getElementById('val-1x-hour').innerText;
            p.display.m2 = document.getElementById('val-2x-month').innerText;
            p.display.h2 = document.getElementById('val-2x-hour').innerText;
            p.display.m3 = document.getElementById('val-3x-month').innerText;
            p.display.h3 = document.getElementById('val-3x-hour').innerText;
        }

        // --- LÓGICA DE CÁLCULO E EDIÇÃO ---
        function calculate() {
            const p = proposals.find(p => p.id === activeId);
            if(!p) return;

            p.cfg.people = parseInt(document.getElementById('cfg-people').value) || 1;
            p.cfg.base = parseFloat(document.getElementById('cfg-base').value) || 0;
            p.cfg.groupDiscount = parseFloat(document.getElementById('cfg-group-discount').value) || 0;
            p.cfg.desc2x = parseFloat(document.getElementById('cfg-desc-2x').value) || 0;
            p.cfg.desc3x = parseFloat(document.getElementById('cfg-desc-3x').value) || 0;

            let ratePerPerson = p.cfg.base;
            if (p.cfg.people > 1) ratePerPerson = p.cfg.base - p.cfg.groupDiscount;

            const hour1x = ratePerPerson * p.cfg.people;
            const hour2x = (ratePerPerson - p.cfg.desc2x) * p.cfg.people;
            const hour3x = (ratePerPerson - p.cfg.desc3x) * p.cfg.people;

            const formatMoney = (val) => val.toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 });

            p.display.m1 = formatMoney(hour1x * 4);
            p.display.h1 = formatMoney(hour1x);
            p.display.m2 = formatMoney(hour2x * 8);
            p.display.h2 = formatMoney(hour2x);
            p.display.m3 = formatMoney(hour3x * 12);
            p.display.h3 = formatMoney(hour3x);

            loadProposalToDOM();
        }

        function updateCurrentProposalName() {
            const p = proposals.find(p => p.id === activeId);
            if(p) {
                p.name = document.getElementById('cfg-student-name').value;
                renderTabs();
            }
        }

        function addProposal() {
            updateActiveFromDOM();
            const newProp = initDefaultProposal();
            newProp.name = 'Nova Proposta';
            proposals.push(newProp);
            
            // Feedback Tátil e Visual
            if(navigator.vibrate) navigator.vibrate([30, 50, 30]);
            
            const panel = document.getElementById('control-panel');
            panel.style.transform = 'scale(0.95)';
            setTimeout(() => panel.style.transform = 'scale(1)', 150);
            
            switchTab(newProp.id);
            showToast("✨ Nova proposta criada! Edite o nome abaixo.");
            
            // Foca no campo de nome para edição rápida
            setTimeout(() => {
                const nameInput = document.getElementById('cfg-student-name');
                if(nameInput) {
                    nameInput.focus();
                    nameInput.select();
                }
            }, 400);
        }

        function removeCurrentProposal() {
            if(proposals.length <= 1) {
                showToast("Você precisa ter pelo menos uma proposta.");
                return;
            }
            if(confirm("Tem certeza que deseja remover esta proposta?")) {
                proposals = proposals.filter(p => p.id !== activeId);
                activeId = proposals[0].id;
                loadProposalToDOM();
                renderTabs();
                showToast("Proposta removida.");
                const panel = document.getElementById('control-panel');
                if (panel.style.display === 'flex') togglePanel();
            }
        }

        // --- MODO EDITOR (GESTO SECRETO) ---
        let secretClicks = 0;
        let secretTimer;

        function handleSecretClick() {
            secretClicks++;
            clearTimeout(secretTimer);
            secretTimer = setTimeout(() => { secretClicks = 0; }, 1000);
            
            if (secretClicks === 3) {
                editorMode = !editorMode;
                document.getElementById('action-controls').style.display = editorMode ? 'flex' : 'none';
                document.querySelectorAll('.amount, .amount-hour').forEach(el => {
                    el.setAttribute('contenteditable', editorMode ? 'true' : 'false');
                });
                showToast(editorMode ? "Modo Editor Ativado!" : "Modo Editor Desativado!");
                secretClicks = 0;
                if(navigator.vibrate) navigator.vibrate([50, 50, 50]);
            }
        }

        // --- UTILITÁRIOS ---
        function togglePanel() {
            const panel = document.getElementById('control-panel');
            if (panel.style.display === 'flex') {
                panel.style.transform = 'translateY(-100%)';
                setTimeout(() => panel.style.display = 'none', 300);
            } else {
                panel.style.display = 'flex';
                setTimeout(() => panel.style.transform = 'translateY(0)', 10);
            }
        }

        function showToast(message) {
            const toast = document.getElementById('toast');
            toast.innerText = message;
            toast.classList.add('show');
            setTimeout(() => { toast.classList.remove('show'); }, 3000);
        }

        function generateLink() {
            updateActiveFromDOM();
            // Compact data for URL
            const data = proposals.map(p => ({
                n: p.name,
                c: [p.cfg.people, p.cfg.base, p.cfg.groupDiscount, p.cfg.desc2x, p.cfg.desc3x],
                d: [p.display.m1, p.display.h1, p.display.m2, p.display.h2, p.display.m3, p.display.h3]
            }));
            
            const encoded = btoa(encodeURIComponent(JSON.stringify(data)));
            const link = window.location.origin + window.location.pathname + '?p=' + encoded;
            
            // Fallback robusto para cópia (funciona mesmo em file:// localmente)
            const copyToClipboard = (text) => {
                if (navigator.clipboard && window.isSecureContext) {
                    return navigator.clipboard.writeText(text);
                } else {
                    let textArea = document.createElement("textarea");
                    textArea.value = text;
                    textArea.style.position = "fixed";
                    textArea.style.left = "-999999px";
                    textArea.style.top = "-999999px";
                    document.body.appendChild(textArea);
                    textArea.focus();
                    textArea.select();
                    return new Promise((res, rej) => {
                        document.execCommand('copy') ? res() : rej();
                        textArea.remove();
                    });
                }
            };

            copyToClipboard(link).then(() => {
                showToast("Link copiado! Abrindo TinyURL...");
                setTimeout(() => {
                    window.open('https://tinyurl.com/app', '_blank');
                }, 1500);
            }).catch(err => {
                prompt("Seu navegador bloqueou a cópia automática. Copie o link abaixo e cole no TinyURL:", link);
                window.open('https://tinyurl.com/app', '_blank');
            });
        }

        function loadFromUrl() {
            const params = new URLSearchParams(window.location.search);
            const p = params.get('p');
            if (p) {
                try {
                    const data = JSON.parse(decodeURIComponent(atob(p)));
                    proposals = data.map(d => ({
                        id: generateId(),
                        name: d.n,
                        cfg: { people: d.c[0]||1, base: d.c[1]||80, groupDiscount: d.c[2]||10, desc2x: d.c[3]||10, desc3x: d.c[4]||20 },
                        display: { m1: d.d[0], h1: d.d[1], m2: d.d[2], h2: d.d[3], m3: d.d[4], h3: d.d[5] }
                    }));
                    activeId = proposals[0].id;
                    return true;
                } catch (e) {
                    console.error("Erro ao ler dados da URL", e);
                }
            }
            return false;
        }

        function exportImage() {
            const controls = document.getElementById('action-controls');
            controls.style.display = 'none';
            const captureArea = document.getElementById('capture-area');
            const originalBorder = captureArea.style.border;
            const originalMargin = captureArea.style.margin;
            const originalRadius = captureArea.style.borderRadius;
            captureArea.style.border = 'none';
            captureArea.style.margin = '0';
            captureArea.style.borderRadius = '0';

            html2canvas(captureArea, { scale: 2, useCORS: true, backgroundColor: '#0f172a' }).then(canvas => {
                const link = document.createElement('a');
                link.download = 'Planos_BenaiBernardo.png';
                link.href = canvas.toDataURL('image/png');
                link.click();
                controls.style.display = 'flex';
                captureArea.style.border = originalBorder;
                captureArea.style.margin = originalMargin;
                captureArea.style.borderRadius = originalRadius;
            }).catch(err => {
                console.error("Erro ao gerar imagem:", err);
                alert("Houve um erro ao gerar a imagem. Tente novamente.");
                controls.style.display = 'flex';
            });
        }

        // --- INICIALIZAÇÃO ---
        if (!loadFromUrl()) {
            proposals = [initDefaultProposal()];
            activeId = proposals[0].id;
        }
        loadProposalToDOM();
        renderTabs();
    </script>
</body>
</html>
